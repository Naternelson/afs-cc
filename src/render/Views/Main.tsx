import { Box, Typography } from "@mui/material";
import { Renderer } from "../Components";

export const MainView = () => {
  return (
    <Box sx={{ flex: 1 }}>
      <Renderer />
    </Box>
  );
};
